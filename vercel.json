{
  "version": 2,
  "builds": [
    { "src": "src/dashboard.ts", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "src/dashboard.ts" },
    { "src": "/(.*)", "dest": "src/dashboard.ts" }
  ]
}
